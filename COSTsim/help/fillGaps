fillGaps               package:COSTsim               R Documentation

_M_e_t_h_o_d _f_o_r _c_o_m_p_l_e_t_i_n_g _g_a_p_s _i_n _s_i_m_u_l_a_t_e_d _d_a_t_a _s_e_t_s

_D_e_s_c_r_i_p_t_i_o_n:

     This method completes dbeSimObj class objects with zeros in non
     sampled age and length dimensions  in order to have objects of the
     same dimension that can be compared

_U_s_a_g_e:

     fillGaps(dbeSimObj, ageMin, ageMax, lenMin, lenMax)

_A_r_g_u_m_e_n_t_s:

dbeSimObj: A dbeOutputSim object

  ageMin: Numeric indicating the minimum age sampled

  ageMax: Numeric indicating the maximum age sampled

  lenMin: Numeric indicating the minimum length sampled

  lenMax: Numeric indicating the maximum length sampled

_V_a_l_u_e:

     It returns the same object but with the age and length dimensions
     completed with zeros

_A_u_t_h_o_r(_s):

     Dorleta Garcia dgarcia@azti.es

